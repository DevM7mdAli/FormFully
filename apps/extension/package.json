{
  "name": "@formfully/extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --import tsx ../../tooling/extension/build.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "@types/jsdom": "^21.1.7",
    "jsdom": "^29.1.1",
    "tailwindcss": "^3.4.1"
  }
}
